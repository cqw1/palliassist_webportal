console.log('pain.js');

$(function() {
    /*
    $('#body-image').mousemove(function(e) {
       // Used to find out where mouse index is.
       console.log((e.pageX - $(this).offset().left) + ', ' + (e.pageY - $(this).offset().top));
    });
    */

    /*
     * Django variables passed in from patient_profile.html
     * django_username: Username of currently logged in user.
     * painSurveys: Array of Objects. created in views.py patient_profile.
     * patientFullName: Full name of patient whose page we're on.
     * token: Twilio IPMessaging access token.
     */
});
